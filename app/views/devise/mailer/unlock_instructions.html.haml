%p= t('devise.mailer.unlock_instructions.hello', id: @resource.callsign)
%p= t('devise.mailer.unlock_instructions.account_locked')
%p= t('devise.mailer.unlock_instructions.instructions')
%p= link_to t('devise.mailer.unlock_instructions.unlock_my_account'), unlock_url(@resource, unlock_token: @token)
