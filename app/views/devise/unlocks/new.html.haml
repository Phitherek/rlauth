.text-center
  %h2= t('devise.lockable.resend_unlock_instructions')
  .row
    .col-md-4.col-md-offset-4
      = form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: 'form-horizontal' }) do |f|
        = devise_error_messages!
        .form-group
          .col-md-12
            = f.label t('parameters.email')
            = f.email_field :email, autofocus: true, class: 'form-control'
        = f.submit t('devise.lockable.resend_unlock_instructions'), class: 'btn btn-default'
  = render "devise/shared/links"
